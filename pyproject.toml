[project]
name = "app-ly"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.40",
    "anthropic>=0.40",
    "python-docx>=1.1",
    "pydantic>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
